import React from "react";

const Button = () => {
  return (
    <div>
      <button
        className="bg-blue-500 btn btn-primary hover:bg-blue-700 text-white"
        type="button"
      >
        Button
      </button>
    </div>
  );
};

export default Button;
